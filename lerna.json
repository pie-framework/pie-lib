{
  "packages": ["packages/*"],
  "version": "independent",
  "verifyAccess": false,
  "command": {
    "publish": {
      "ignoreChanges": ["yarn.lock", "CHANGELOG.json", "CHANGELOG.md", "LICENSE.md"],
      "message": "[ci skip] chore: publish"
    }
  },
  "$schema": "node_modules/lerna/schemas/lerna-schema.json"
}
