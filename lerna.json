{
  "lerna": "3.11.0",
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["yarn.lock", "CHANGELOG.json"],
      "message": "[skip travis] chore: publish"
    }
  }
}
